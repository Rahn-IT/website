server {
    listen 80;

    root /var/www/it-rahn.de; 

    location / {
        index app.html;
        try_files $uri $uri/ /app.html;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}